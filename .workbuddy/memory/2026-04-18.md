# 2026-04-18 工作日志

## 场景切换系统

### 完成内容
- **SceneChanger.cs**：Collider2D触发器 + 淡入淡出动画（FadeToDark）+ 定点传送（newPlayerPosition）
- **GameManager.cs**：单例模式 + DontDestroyOnLoad 持久化
- **持久化对象**：Player、Canvas（含Global Light 2D）、EventSystem
- Confiner 从持久化中移除（场景独立，不能保留）
- 每个场景单独配置 Confiner + PolygonCollider2D 边界

### 待改进（明天做）
- GameManager 的 persistentObjects 改为运行时 FindWithTag 获取，避免场景重载后引用失效

## SPUM 素材测试

- 测试项目路径：D:\Text\Gametry-Assets-Test
- 7种动画状态：IDLE/MOVE/ATTACK/DAMAGED/DEBUFF/DEATH/OTHER
- PlayerObj.cs：移动逻辑 + 转身逻辑（scale.x 翻转）
- URP 项目需要 Global Light 2D 才能正常渲染 SPUM 精灵
- 导出 Prefab 可替换 Gametry 主项目的 NPC 精灵

## Cainos Top Down Basic

- 俯视角高度分层系统（Sorting Layers）
- Tilemap 画地图
- Stair Layer Trigger 处理楼梯遮挡

## 踩坑记录

- URP 项目需设置 Graphics → Default Render Pipeline（Pipeline Asset），否则黑屏
- EventSystem 每个场景都要有，或用 DontDestroyOnLoad 保留
- 切换场景时 Camera 边界自动刷新：SceneManager.sceneLoaded + FindWithTag("Confiner")
- 持久化对象拖 Inspector 引用会失效，改为运行时 FindWithTag 获取
